---
- name: Get ansible_user home directory (Debian)
  ansible.builtin.shell: 'set -o pipefail && getent passwd "{{ ansible_user }}" | cut -d: -f6'
  register: tools_ansible_home_result_debian
  changed_when: false
  when:
    - ansible_os_family == "Debian"
  tags:
    - config-tool

- name: Get ansible_user home directory (Darwin)
  ansible.builtin.shell: set -o pipefail && finger "{{ ansible_user }}" | awk '/^Directory/ {print $2}'
  register: tools_ansible_home_result_darwin
  changed_when: false
  when:
    - ansible_os_family == "Darwin"
  tags:
    - config-tool

- name: Set the fact for the other scripts to use
  ansible.builtin.set_fact:
    tools_ansible_home: "{{ tools_ansible_home_result_debian.stdout | default(tools_ansible_home_result_darwin.stdout | default('')) }}"
  changed_when: false
  when: (tools_ansible_home_result_debian.stdout is defined) or (tools_ansible_home_result_darwin.stdout is defined)
  tags:
    - config-tool

- name: Get os in lowercase
  ansible.builtin.shell: set -o pipefail && uname | tr '[:upper:]' '[:lower:]'
  register: tools_os_result
  changed_when: false
  tags:
    - config-tool

- name: Get target arch
  ansible.builtin.shell: set -o pipefail && uname -m | sed -e 's/x86_64/amd64/' -e 's/\(arm\)\(64\)\?.*/\1\2/' -e 's/aarch64$/arm64/'
  register: tools_arch_result
  changed_when: false
  tags:
    - config-tool

- name: For all
  ansible.builtin.import_tasks: common.yml

- name: For debian
  ansible.builtin.import_tasks: debian.yml
  when:
    - ansible_os_family == "Debian"

- name: For darwin
  ansible.builtin.import_tasks: darwin.yml
  when:
    - ansible_os_family == "Darwin"
