---
- name: Get ansible_user home directory (Debian)
  ansible.builtin.shell: 'set -o pipefail && getent passwd "{{ ansible_user }}" | cut -d: -f6'
  register: dotfiles_ansible_home_result_debian
  changed_when: false
  when:
    - ansible_os_family == "Debian"
  tags:
    - config-base
    - config-dotfile

- name: Get ansible_user home directory (Darwin)
  ansible.builtin.shell: set -o pipefail && finger "{{ ansible_user }}" | awk '/^Directory/ {print $2}'
  register: dotfiles_ansible_home_result_darwin
  changed_when: false
  when:
    - ansible_os_family == "Darwin"
  tags:
    - config-base
    - config-dotfile

- name: Set the fact for the other scripts to use
  ansible.builtin.set_fact:
    dotfiles_ansible_home: "{{ dotfiles_ansible_home_result_debian.stdout | default(dotfiles_ansible_home_result_darwin.stdout | default('')) }}"
  changed_when: false
  when: (dotfiles_ansible_home_result_debian.stdout is defined) or (dotfiles_ansible_home_result_darwin.stdout is defined)
  tags:
    - config-base
    - config-dotfile

- name: Place Default Config
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/{{ item }}
    dest: "{{ dotfiles_ansible_home }}/.{{ item }}"
    mode: "0755"
    backup: true
  with_items:
    - bash_profile
    - bashrc
  when:
    - ansible_os_family == "Debian"
  tags:
    - config-base
    - config-dotfile

- name: Place Default Config
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/{{ item }}
    dest: "{{ dotfiles_ansible_home }}/.{{ item }}"
    mode: "0755"
    backup: true
  with_items:
    # - zshrc
    - bash_profile
    - bashrc
  when:
    - ansible_os_family == "Darwin"
  tags:
    - config-base
    - config-dotfile

- name: Install bash-completion
  become: true
  become_user: "{{ ansible_user }}"
  community.general.homebrew:
    name:
      - bash-completion
    state: present
  when:
    - ansible_os_family == "Darwin"
  tags:
    - config-base
    - config-dotfile

- name: Install bash-completion
  become: true
  ansible.builtin.apt:
    name:
      - bash-completion
    state: present
  when:
    - ansible_os_family == "Debian"
  tags:
    - config-base
    - config-dotfile

- name: Fetch git-prompt.sh
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.get_url:
    url: https://raw.githubusercontent.com/git/git/master/contrib/completion/git-prompt.sh
    dest: "{{ dotfiles_ansible_home }}/.git-prompt.sh"
    mode: "0664"
  tags:
    - config-base
    - config-dotfile

- name: Fetch aws-ps1
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.git: # noqa: latest
    repo: https://github.com/eddiecorrigall/awscli-ext
    dest: "{{ dotfiles_ansible_home }}/awscli-ext"
  tags:
    - config-base
    - config-dotfile

- name: Fetch kube-ps1
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.git: # noqa: latest
    repo: https://github.com/jonmosco/kube-ps1
    dest: "{{ dotfiles_ansible_home }}/kube-ps1"
  tags:
    - config-base
    - config-dotfile

- name: Fetch z.sh
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.git: # noqa: latest
    repo: https://github.com/rupa/z
    dest: "{{ dotfiles_ansible_home }}/z"
  tags:
    - config-base
    - config-dotfile

- name: Place Shell Files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/bash.d/"
    dest: "{{ dotfiles_ansible_home }}/.bash.d/"
    mode: "0755"
    directory_mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Create target directory for nvim
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.file:
    path: "{{ dotfiles_ansible_home }}/.config/nvim"
    state: directory
    mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place init.lua for nvim
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/nvim.lua
    dest: "{{ dotfiles_ansible_home }}/.config/nvim/init.lua"
    mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place nvim lua files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/nvim/"
    dest: "{{ dotfiles_ansible_home }}/.config/nvim/lua/"
    mode: "0755"
    directory_mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Create target directory for claude code fragments
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.file:
    path: "{{ dotfiles_ansible_home }}/.claude"
    state: directory
    mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place claude code setting global settings
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: "files/claude/{{ item }}"
    dest: "{{ dotfiles_ansible_home }}/.claude/{{ item }}"
    mode: "0755"
  with_items:
    - CLAUDE.md
    - mcp.json
    - settings.json
  tags:
    - config-base
    - config-dotfile

- name: Place claude code skill files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/claude/skills/"
    dest: "{{ dotfiles_ansible_home }}/.claude/skills/"
    mode: "0755"
    directory_mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place claude code agent files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/claude/agents/"
    dest: "{{ dotfiles_ansible_home }}/.claude/agents/"
    mode: "0755"
    directory_mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place claude code hook files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/claude/hooks/"
    dest: "{{ dotfiles_ansible_home }}/.claude/hooks/"
    mode: "0755"
    directory_mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place claude code rule files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/claude/rules/"
    dest: "{{ dotfiles_ansible_home }}/.claude/rules/"
    mode: "0755"
    directory_mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Create target directory for translate-shell
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.file:
    path: "{{ dotfiles_ansible_home }}/.translate-shell"
    state: directory
    mode: "0755"
    recurse: true
  tags:
    - config-base
    - config-dotfile

- name: Place .init.trans
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/init.trans
    dest: "{{ dotfiles_ansible_home }}/.translate-shell/init.trans"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Create target directory for mise
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.file:
    path: "{{ dotfiles_ansible_home }}/.config/mise/conf.d"
    state: directory
    mode: "0755"
  tags:
    - config-base
    - config-dotfile
    - skip_ansible_lint

- name: Place mise config
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: files/mise.toml
    dest: "{{ dotfiles_ansible_home }}/.config/mise/config.toml"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Place .tmux.conf
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/tmux.conf
    dest: "{{ dotfiles_ansible_home }}/.tmux.conf"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Fetch tpm
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.git: # noqa: latest
    repo: https://github.com/tmux-plugins/tpm
    dest: "{{ dotfiles_ansible_home }}/.tmux/plugins/tpm"
  tags:
    - config-base
    - config-dotfile

- name: Create target directory for alacritty
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.file:
    path: "{{ dotfiles_ansible_home }}/.config/alacritty"
    state: directory
    mode: "0755"
  tags:
    - config-base
    - config-dotfile
    - skip_ansible_lint

- name: Place alacritty.yml
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/alacritty.yml
    dest: "{{ dotfiles_ansible_home }}/.config/alacritty/alacritty.yml"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Place my.cnf
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/my.cnf
    dest: "{{ dotfiles_ansible_home }}/.my.cnf"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Place git config files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: "files/git/{{ item }}"
    dest: "{{ dotfiles_ansible_home }}/{{ item }}"
    mode: "0755"
    backup: true
  with_items:
    - .gitconfig
    - .gitignore_global
  tags:
    - config-base
    - config-dotfile

- name: Create target directory for gwq
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.file:
    path: "{{ dotfiles_ansible_home }}/.config/gwq"
    state: directory
    mode: "0755"
  tags:
    - config-base
    - config-dotfile

- name: Place gwq config files
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.copy:
    src: "files/git/gwq.toml"
    dest: "{{ dotfiles_ansible_home }}/.config/gwq/config.toml"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Place .ctags
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/ctags
    dest: "{{ dotfiles_ansible_home }}/.ctags"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Place .globalrc
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/globalrc
    dest: "{{ dotfiles_ansible_home }}/.globalrc"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile

- name: Place .tigrc
  become: true
  become_user: "{{ ansible_user }}"
  ansible.builtin.template:
    src: files/tigrc
    dest: "{{ dotfiles_ansible_home }}/.tigrc"
    mode: "0755"
    backup: true
  tags:
    - config-base
    - config-dotfile
